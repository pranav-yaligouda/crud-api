apiVersion: v1
kind: ConfigMap
metadata:
  name: crud-api-config
  labels:
    app: crud-api
data:
  # Non-sensitive runtime configuration. Values must be strings.
  NODE_ENV: "production"
  PORT: "5000"
  # Public base URL (update for a real domain or LB)
  API_BASE_URL: "http://localhost:3000"

