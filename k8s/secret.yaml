apiVersion: v1
kind: Secret
metadata:
  name: crud-api-secret
  labels:
    app: crud-api
type: Opaque
stringData:
  # Use stringData for plaintext values locally; Kubernetes will store them base64-encoded.
  MONGODB_URI: "change_me"
  JWT_SECRET: "change_me"

